Both the programs follow the following logic:

1) Program 1 labelled as "py1": Is a program that opens a file and reads it elements as per the file given in the "Task 1" i alloted the name "sample.txt" to my file. It reads the file line by line by using